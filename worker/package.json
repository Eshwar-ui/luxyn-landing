{
  "name": "luxyn-contact-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that emails LUXYN contact-form submissions via Resend.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5",
    "wrangler": "^3.90.0"
  }
}
